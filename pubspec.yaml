name: gradient_like_css
description: The gradient_like_css package for Flutter allows you to experience CSS-like gradients in your Flutter app.
version: 0.5.1
homepage: https://github.com/azukisiromochi/gradient_like_css

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=2.5.3"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
